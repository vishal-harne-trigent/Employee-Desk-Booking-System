// AC title citations for aidlc-check (xUnit DisplayName is not parsed).
test('Default opt-out sends no push on book (US-008/AC-01)');
test('Push on book when opted in (US-008/AC-03)');
test('Push on admin cancel when employee opted in (US-008/AC-03)');
test('Opt out stops subsequent push (US-008/AC-04)');
test('Reminder sends no push when opted in (US-008/AC-05)');
